<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 工具类日志文件 -->
    <appender name="TOOL_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/tools.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/tools.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 过滤日志 -->
    <appender name="FILTER_FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/topic-memory-filter.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 工具类 Logger -->
    <logger name="com.westart.ai.westart.service.tool" level="INFO" additivity="false">
        <appender-ref ref="TOOL_FILE"/>
    </logger>

    <!-- 过滤日志 -->
    <logger name="TOPIC_MEMORY_FILTER_LOG" level="INFO" additivity="false">
        <appender-ref ref="FILTER_FILE"/>
    </logger>

    <!-- 默认 Logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
